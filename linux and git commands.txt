Command lines important in the linux point of view:-
Let’s explore some other commands which will be of help in upcoming milestones.


13. Output Redirection (> ,>>)

The output from a command normally intended for standard output can be easily diverted to a file instead. This capability is known as output redirection.
If the notation > file is appended to any command that normally writes its output to standard output, the output of that command will be written to file instead of your terminal.
You can use >> operator to append the output in an existing file
image

https://www.tecmint.com/exploring-proc-file-system-in-linux/
https://www.thegeekdiary.com/basic-ls-command-examples-in-linux/
https://www.cyberciti.biz/faq/linux-command-to-find-the-system-configuration-and-hardware-information/
https://www.tecmint.com/exploring-proc-file-system-in-linux/


Useful shortcuts.
up arrow key → will bring up the last command that was executed. Each press will bring up the previous command executed.
history → will print out a list of the previous commands executed.
tab key → pressing the tab key can be used to auto-complete the directory and file names while typing paths or filenames.
Linux directory structure


Within this byte, you learnt the following:


Pwd :You can use the pwd command to print the working directory.
To list the contents of a directory, you use the ls command (short for list).
You can change to a different directory using the cd command (short for change directory).
Arguments is an input given as part of terminal commands.There can be one or more arguments/parameters.
Options modifies the behavior of a command.
Useful shortcuts.
up arrow key → will bring up the last command that was executed. Each press will bring up the previous command executed.
history → will print out a list of the previous commands executed.
tab key → pressing the tab key can be used to auto-complete the directory and file names while typing paths or filenames.
Linux directory structure

The command mkdir stands for “make directory”.
touch is used to create a file without any content.
Cat(concatenate) command is very frequently used in Linux. It reads data from the file and gives their content as output.
cp command is used to copy the contents of one file to another.
mv is used to move one or more files or directories from one place to another in a file system like UNIX.
rm command is used to remove files.
rmdir command is used to remove folders.
The echo command in linux is used to display line of text/string that are passed as an argument .
File permissions:


git commands:-
1. What happened after you resolved the merge conflict and did a "git add README.md" followed by “git commit”?
2. https://ohshitgit.com/
3.https://www.toptal.com/git/the-advanced-git-guide 